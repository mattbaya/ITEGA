# OS files
.DS_Store
Thumbs.db

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node (when dashboard is built)
node_modules/
dist/

# Python (when ALS services are built)
__pycache__/
*.pyc
.venv/
venv/

# Java (when Keycloak SPI is built)
target/
*.class

# Environment secrets
.env
.env.*

# Reference material: source documents, correspondence, and Bill's working
# demo scripts. Kept local only -- this repository is public, and the
# correspondence in particular is not ours to publish.
reference/

# Vite build metadata
*.tsbuildinfo

# Plugin dependencies are vendored at package time, not committed
src/wordpress-plugin/vendor/

# Built decks and their narrated videos. Large, regenerable from
# scratchpad templates, and the Bill report names a publisher who has not
# yet agreed publicly -- so they stay local rather than going into a
# repository Bill's column links to.
newshare-pitch.html
newshare-bill-report.html
*.mp4

# Music beds: commercial recordings, not ours to redistribute.
*.m4a
*.mp3
